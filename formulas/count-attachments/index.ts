export const meta = import.meta;
export const formulaSourceCodePath = `${import.meta.url}/${index.formula}`;
// const formulaSource = await fetch(`${meta.url}/${index.formula}`).then(r => r.text());
export metaJson from './meta.json';
export {meta}

